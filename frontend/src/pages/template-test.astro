---
import DefaultLayout from "../layouts/default.astro";
---
<DefaultLayout>
  <p>This is an example of a template.</p>
  <p>Received request context:</p>
  <p><pre><code>{JSON.stringify(Astro.locals.django.context)}</code></pre></p>
</DefaultLayout>
